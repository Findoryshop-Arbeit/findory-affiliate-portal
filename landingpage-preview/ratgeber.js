(() => { const list = document.querySelector('#guide-list'); const guides = [...(window.FINDORY_GUIDES || []).reduce((unique, guide) => unique.set(guide.id, guide), new Map()).values()]; list.innerHTML = guides.map((guide) => `<article class="guide-card"><p>${guide.readTime}</p><h2>${guide.title}</h2><p>${guide.intro}</p><a href="ratgeber-beitrag.html?guide=${encodeURIComponent(guide.id)}">Vergleich öffnen →</a></article>`).join(''); })();
